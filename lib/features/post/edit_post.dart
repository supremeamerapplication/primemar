import 'package:flutter/material.dart';

class EditPost extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Edit Post')),
      body: Center(child: Text('Edit post form')),
    );
  }
}
